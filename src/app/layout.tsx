import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/theme-provider";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Premium Cleaning Services London | Luxury Housekeeping & Commercial Cleaning | Voila Cleaners",
  description: "Voila Cleaners provides premium cleaning services in London, including luxury housekeeping, deep cleaning, commercial cleaning, and serviced apartment cleaning. Trusted, discreet, and detail-focused. Request your bespoke quote today.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="light"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
